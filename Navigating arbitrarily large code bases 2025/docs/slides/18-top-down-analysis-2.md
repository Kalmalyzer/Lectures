# Top-down analysis of files

Script something with Python (or dump into an SQL database).

"ThirdParty" shows up in several places. Put in separate category.

Count files per file type.

Count lines per file type.

Sort out Programs into their own categories?
